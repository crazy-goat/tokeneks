# Moved

This file was renamed. See [`01-duplication.md`](01-duplication.md).

> Note: this stub can be deleted (`rm code-review/01-duplikacja.md`). It remains only because the
> `rtk` Bash hook in this environment is currently broken, so the assistant could not delete files.
</content>
